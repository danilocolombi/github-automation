name: Project - List

on: 
  workflow_dispatch:
    inputs:
      owner:
        description: 'Project Owner'
        required: true
        default: 'danilocolombi'

jobs:
  create:
    runs-on: ubuntu-latest
    
    steps:   
      - name: List Projects
        run: gh project list
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN_CLASSIC }} --owner 
